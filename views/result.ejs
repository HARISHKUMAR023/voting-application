<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>result</title>
</head>

<body>
    <div>

        <% for (const voting of vote){ %>




            <% if(voting.nominee==3){ %>
                <h3>
                    The <%=voting.name %> is voted to the Divya dharshini G S is <%=voting.nominee.length %> vote is
                            Recoded
                </h3>

                <% } %>
                    <% if(voting.nominee==1){ %>
                        <h3>
                            The <%=voting.name %> is voted to the Akshaya D is <%=voting.nominee.length %> vote is
                                    Recoded

                        </h3>


                        <% } %>
                            <% if(voting.nominee==2){ %>
                                <h3>
                                    The <%=voting.name %> is voted to the Karthikeyan P C is <%=voting.nominee.length %>
                                            vote is Recoded
                                </h3>

                                <% } %>
                                    <% if(voting.nominee==4){ %>
                                        <h3>
                                            The <%=voting.name %> is voted to the Aruna S is <%=voting.nominee.length %>
                                                    vote is Recoded
                                        </h3>

                                        <% } %>

                                            <% } %>



    </div>


</body>

</html>